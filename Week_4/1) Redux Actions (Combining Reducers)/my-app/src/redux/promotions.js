//promotions reducer

import { PROMOTIONS } from '../shared/promotions';

export const Promotions = (state = PROMOTIONS, action) => {  //taking initial state as DISHES
    switch(action.type) {
        default:
            return state;
    }
}